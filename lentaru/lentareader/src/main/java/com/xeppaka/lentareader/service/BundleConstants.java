package com.xeppaka.lentareader.service;

public enum BundleConstants {
	/*
	 * id of the original request
	 */
	KEY_REQUEST_ID,
	/*
	 * what was done
	 */
	KEY_ACTION,
	/*
	 * what type of news
	 */
	KEY_NEWS_TYPE,
	/*
	 * in case of fail: details in the exception
	 */
	KEY_EXCEPTION,
	/*
	 *  if some objects was updated or created this is list of ids of that objects
	 */
	KEY_IDS
}
